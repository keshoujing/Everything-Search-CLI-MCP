{
  "name": "everything-search-cli-mcp",
  "version": "1.0.1",
  "description": "MCP server wrapping es.exe (Everything Search CLI) for Windows file search",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "everything-search-cli-mcp": "dist/index.js"
  },
  "files": [
    "dist/",
    "bin/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "csv-parse": "^6.2.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "os": ["win32"],
  "keywords": ["mcp", "everything", "search", "windows", "files"]
}
